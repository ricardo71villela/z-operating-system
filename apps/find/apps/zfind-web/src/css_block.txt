:root{
  --white:#FFFFFF; --black:#0A0A0A;
  --gray-50:#F8F8F6; --gray-100:#F0EFE9; --gray-200:#E2E0D8;
  --gray-300:#C8C6BC; --gray-400:#9E9C94; --gray-500:#6E6C64;
  --gray-700:#3A3834; --gray-900:#1A1916;
  --gold:#B8935A; --gold-light:#D4AF7A; --gold-dark:#8B6B3A; --gold-pale:#F5EDD8;
  --whatsapp:#25D366;
  --radius:2px;
  --ease:cubic-bezier(0.25,0.46,0.45,0.94);
  --shadow-soft:0 4px 40px rgba(0,0,0,0.04);
  --shadow-lift:0 24px 80px rgba(0,0,0,0.08);
}
*{box-sizing:border-box; margin:0; padding:0;}
body{
  font-family:'DM Sans', sans-serif; font-weight:300;
  color:var(--gray-900); background:var(--white);
  -webkit-font-smoothing:antialiased;
}
h1,h2,h3,h4,.serif{ font-family:'Cormorant Garamond', serif; font-weight:400; letter-spacing:0; }
/* Functional data, not a decorative headline — Cormorant Garamond's
   "0" is visually near-identical to a lowercase "o" at this weight,
   confusing for a real count like "0 opportunities". Sans-serif here
   only, unlike every other h2 on the site. */
#search-results-title{ font-family:'DM Sans', sans-serif; font-weight:500; }
.eyebrow{
  font-family:'DM Sans',sans-serif; font-size:0.72rem; letter-spacing:0.18em;
  text-transform:uppercase; color:var(--gold-dark); font-weight:500;
}
a{ color:inherit; text-decoration:none; }
img{ max-width:100%; display:block; }
button{ font-family:inherit; cursor:pointer; border:none; background:none; }

/* ---------- layout shell ---------- */
.wrap{ max-width:1300px; margin:0 auto; padding:0 32px; }
header.site{
  position:sticky; top:0; z-index:40; background:rgba(255,255,255,0.94);
  backdrop-filter:blur(8px); border-bottom:1px solid var(--gray-200);
}
.nav-row{ display:flex; align-items:center; gap:40px; height:76px; }
.logo{ font-family:'Cormorant Garamond',serif; font-size:1.7rem; font-weight:500; letter-spacing:0.02em; white-space:nowrap; display:flex; align-items:center; gap:9px; }
.logo em{ color:var(--gold); font-style:normal; }
.zf-mark{ width:30px; height:30px; flex:none; display:block; }
.logo.lg .zf-mark{ width:52px; height:52px; }
footer.site .logo em{ color:var(--gold-light); }
.nav-links{ display:flex; gap:28px; flex:1; }
.nav-links button{
  font-size:0.86rem; color:var(--gray-700); padding:8px 2px; position:relative;
  transition:color 0.3s var(--ease);
}
.nav-links button.active, .nav-links button:hover{ color:var(--black); }
.nav-links button.active::after{
  content:""; position:absolute; left:0; right:0; bottom:-2px; height:2px; background:var(--gold);
}
.nav-actions{ display:flex; gap:14px; align-items:center; }
.btn{
  font-size:0.78rem; letter-spacing:0.12em; text-transform:uppercase; font-weight:500;
  padding:13px 26px; border-radius:var(--radius); transition:all 0.3s var(--ease);
  display:inline-flex; align-items:center; gap:8px; white-space:nowrap;
}
.btn-gold{ background:var(--gold); color:var(--white); }
.btn-gold:hover{ background:var(--gold-dark); transform:translateY(-2px); box-shadow:0 10px 30px rgba(184,147,90,0.35); }
.btn-outline{ background:transparent; border:1px solid var(--gray-300); color:var(--gray-900); }
.btn-outline:hover{ border-color:var(--gray-900); }
.btn-ghost{ font-size:0.8rem; color:var(--gray-700); }
.btn-ghost:hover{ color:var(--black); }

/* ---------- view switching ---------- */
.view{ display:none; }
.view.active{ display:block; }

/* ---------- hero / search ---------- */
.hero{ padding:90px 0 60px; background:var(--gray-50); border-bottom:1px solid var(--gray-200); }
.hero .eyebrow{ display:block; margin-bottom:18px; }
.hero h1{ font-size:3.6rem; line-height:1.05; max-width:820px; font-weight:400; }
.hero h1 i{ font-style:italic; color:var(--gold-dark); }
.hero p.lead{ margin-top:22px; max-width:560px; font-size:1.05rem; color:var(--gray-700); line-height:1.6; }

.searchbar{
  margin-top:44px; background:var(--white); border:1px solid var(--gray-200); border-radius:var(--radius);
  box-shadow:var(--shadow-soft); display:flex; gap:0; overflow:hidden; max-width:900px;
}
.searchbar .cat-tabs{ display:flex; border-bottom:1px solid var(--gray-100); }
.searchbar-inner{ display:flex; flex-direction:column; width:100%; }
.cat-tabs button{
  flex:1; padding:16px 10px; font-size:0.8rem; letter-spacing:0.06em; color:var(--gray-500);
  border-bottom:2px solid transparent; transition:all .25s var(--ease);
}
.cat-tabs button.active{ color:var(--gray-900); border-bottom-color:var(--gold); background:var(--gray-50); }
.search-fields{ display:flex; align-items:center; gap:0; padding:8px; }
.search-fields input, .search-fields select{
  border:none; background:none; font-family:'DM Sans'; font-size:0.92rem; color:var(--gray-900);
  padding:12px 16px; outline:none;
}
/* The location input needs more room than the budget dropdown — equal
   flex:1 on both truncated the placeholder ("Location — city, zone or
   address") while the dropdown sat with unused space. */
.search-fields input{ flex:3; min-width:0; }
.search-fields select{ flex:1; }
.search-fields .divider{ width:1px; height:26px; background:var(--gray-200); }
.search-fields button.go{
  background:var(--gold); color:#fff; padding:14px 30px; border-radius:var(--radius);
  font-size:0.78rem; letter-spacing:0.12em; text-transform:uppercase; margin:4px; flex:none;
}

.principle-strip{ display:flex; gap:0; margin-top:60px; border-top:1px solid var(--gray-200); }
.principle-strip .p{ flex:1; padding:26px 28px 0; border-left:1px solid var(--gray-200); }
.principle-strip .p:first-child{ border-left:none; }
.principle-strip .p .num{ font-family:'Cormorant Garamond'; font-style:italic; color:var(--gold); font-size:1.1rem; }
.principle-strip .p h4{ font-size:1.25rem; margin-top:6px; font-weight:400; }
.principle-strip .p p{ font-size:0.86rem; color:var(--gray-500); margin-top:6px; line-height:1.5; }

/* ---------- section generic ---------- */
section.block{ padding:76px 0; }
.block-head{ display:flex; justify-content:space-between; align-items:flex-end; margin-bottom:36px; gap:24px; }
.block-head h2{ font-size:2.1rem; font-weight:400; }
.block-head p{ color:var(--gray-500); font-size:0.92rem; margin-top:8px; max-width:460px; }

.tabs-row{ display:flex; gap:8px; margin-bottom:34px; flex-wrap:wrap; }
.pill{
  padding:9px 18px; border-radius:var(--radius); border:1px solid var(--gray-200); font-size:0.8rem;
  color:var(--gray-700); transition:all .25s var(--ease);
}
.pill.active, .pill:hover{ border-color:var(--gray-900); color:var(--gray-900); }

/* ---------- cards ---------- */
.grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:28px; }
.card{
  border:1px solid var(--gray-200); border-radius:var(--radius); overflow:hidden; cursor:pointer;
  transition:box-shadow .35s var(--ease), transform .35s var(--ease); background:#fff;
}
.card:hover{ box-shadow:var(--shadow-lift); transform:translateY(-4px); }
.card .thumb{
  height:210px; background:linear-gradient(135deg,var(--gray-200),var(--gray-100));
  position:relative; display:flex; align-items:center; justify-content:center; color:var(--gray-400); font-size:0.78rem;
}
.badge{
  position:absolute; top:12px; left:12px; background:rgba(10,10,10,0.82); color:#fff;
  font-size:0.68rem; letter-spacing:0.1em; text-transform:uppercase; padding:6px 11px; border-radius:var(--radius);
}
.badge.gold{ background:var(--gold); }
.badge.verified{ background:rgba(10,10,10,0.82); display:flex; align-items:center; gap:5px; }
.card .body{ padding:20px; }
.card .price{ font-family:'Cormorant Garamond'; font-size:1.5rem; }
.card .loc{ font-size:0.82rem; color:var(--gray-500); margin-top:4px; }
.card .meta{ display:flex; gap:14px; margin-top:14px; font-size:0.78rem; color:var(--gray-500); border-top:1px solid var(--gray-100); padding-top:12px; }
.card .facts-count{ margin-top:10px; font-size:0.72rem; color:var(--gold-dark); letter-spacing:0.04em; }

/* ---------- info blocks / fact vs estimate ---------- */
.tag{
  display:inline-flex; align-items:center; gap:6px; font-size:0.66rem; letter-spacing:0.1em; text-transform:uppercase;
  padding:4px 9px; border-radius:var(--radius); font-weight:500;
}
.tag-fact{ background:var(--gray-100); color:var(--gray-700); }
.tag-estimate{ background:var(--gold-pale); color:var(--gold-dark); }
.tag-verified{ background:var(--black); color:#fff; }

/* ---------- property detail ---------- */
.detail-hero{ background:var(--gray-900); color:var(--gray-100); padding:56px 0 40px; }
.detail-hero .eyebrow{ color:var(--gold-light); }
.detail-hero h1{ font-size:2.6rem; color:#fff; margin-top:10px; }
.detail-hero .loc-row{ display:flex; gap:18px; align-items:center; margin-top:12px; color:var(--gray-400); font-size:0.9rem; }
.detail-hero .price-tag{ font-family:'Cormorant Garamond'; font-size:2rem; color:var(--gold-light); margin-top:20px; }

.detail-layout{ display:grid; grid-template-columns:1.6fr 1fr; gap:48px; padding:48px 0; }
.gallery{ height:420px; background:linear-gradient(135deg,var(--gray-200),var(--gray-50)); border-radius:var(--radius); margin-bottom:34px; display:flex; align-items:center; justify-content:center; color:var(--gray-400); }
.facts-grid{ display:grid; grid-template-columns:repeat(3,1fr); gap:0; border:1px solid var(--gray-200); border-radius:var(--radius); margin-bottom:40px; }
.fact{ padding:20px; border-right:1px solid var(--gray-200); border-bottom:1px solid var(--gray-200); }
.fact:nth-child(3n){ border-right:none; }
.fact .k{ font-size:0.7rem; letter-spacing:0.08em; text-transform:uppercase; color:var(--gray-500); }
.fact .v{ font-family:'Cormorant Garamond'; font-size:1.35rem; margin-top:6px; }

.section-title{ font-size:1.4rem; margin-bottom:16px; display:flex; align-items:center; justify-content:space-between; }
.info-card{ border:1px solid var(--gray-200); border-radius:var(--radius); padding:22px; margin-bottom:16px; }
.info-card .row{ display:flex; justify-content:space-between; align-items:flex-start; gap:16px; padding:9px 0; border-bottom:1px solid var(--gray-100); font-size:0.88rem; }
.info-card .row:last-child{ border-bottom:none; }
.info-card .row .label{ color:var(--gray-500); }
.info-card .row .val{ text-align:right; }

.sidebar-card{ border:1px solid var(--gray-200); border-radius:var(--radius); padding:24px; margin-bottom:20px; }
.sidebar-sticky{ position:sticky; top:96px; align-self:start; }
.sidebar-card h4{ font-size:1.15rem; margin-bottom:14px; }
.sim-row{ display:flex; justify-content:space-between; font-size:0.85rem; padding:8px 0; border-bottom:1px solid var(--gray-100); }
.sim-row.total{ font-weight:500; color:var(--gray-900); border-bottom:none; padding-top:12px; }
.disclaimer{ font-size:0.72rem; color:var(--gray-400); margin-top:14px; line-height:1.5; display:flex; gap:6px; }

/* ---------- development units table ---------- */
.units-table{ width:100%; border-collapse:collapse; margin-top:10px; }
.units-table th{ text-align:left; font-size:0.7rem; letter-spacing:0.08em; text-transform:uppercase; color:var(--gray-500); padding:10px 14px; border-bottom:1px solid var(--gray-300); }
.units-table td{ padding:14px 14px; border-bottom:1px solid var(--gray-100); font-size:0.88rem; }
.units-table tr:hover td{ background:var(--gray-50); }
.status-dot{ display:inline-flex; align-items:center; gap:6px; font-size:0.78rem; }
.status-dot::before{ content:""; width:7px; height:7px; border-radius:50%; background:var(--gold); }
.status-dot.sold::before{ background:var(--gray-300); }
.status-dot.reserved::before{ background:var(--gray-500); }

/* ---------- land scenarios ---------- */
.scenario-card{ border:1px solid var(--gray-200); border-radius:var(--radius); padding:22px; margin-bottom:14px; }
.scenario-card .top{ display:flex; justify-content:space-between; align-items:center; }
.scenario-card h4{ font-size:1.2rem; }

/* ---------- enquiry modal ---------- */
.modal-overlay{
  position:fixed; inset:0; background:rgba(10,10,10,0.55); z-index:100; display:none;
  align-items:center; justify-content:center; padding:24px;
}
.modal-overlay.active{ display:flex; }
.modal{
  background:#fff; max-width:560px; width:100%; border-radius:var(--radius); padding:40px;
  max-height:88vh; overflow-y:auto; box-shadow:var(--shadow-lift);
}
.modal h3{ font-size:1.7rem; }
.modal .sub{ color:var(--gray-500); font-size:0.88rem; margin-top:8px; }
.contact-options{ display:grid; gap:14px; margin-top:26px; }
.contact-opt{
  border:1px solid var(--gray-200); border-radius:var(--radius); padding:20px; cursor:pointer;
  transition:all .25s var(--ease); text-align:left;
}
.contact-opt:hover{ border-color:var(--gold); }
.contact-opt.selected{ border-color:var(--gold); background:var(--gold-pale); }
.contact-opt .row{ display:flex; justify-content:space-between; align-items:center; }
.contact-opt h5{ font-size:1.05rem; font-family:'DM Sans'; font-weight:500; }
.contact-opt p{ font-size:0.8rem; color:var(--gray-500); margin-top:6px; line-height:1.5; }
.qual-form{ display:none; margin-top:22px; padding-top:22px; border-top:1px solid var(--gray-100); }
.qual-form.active{ display:block; }
.qual-form label{ font-size:0.75rem; letter-spacing:0.05em; text-transform:uppercase; color:var(--gray-500); display:block; margin-bottom:6px; margin-top:14px; }
.qual-form input, .qual-form select{
  width:100%; padding:12px 14px; border:1px solid var(--gray-200); border-radius:var(--radius); font-family:'DM Sans'; font-size:0.9rem;
}
.close-x{ position:absolute; top:24px; right:28px; font-size:1.4rem; color:var(--gray-400); }

/* ---------- partner ---------- */
.partner-header{ display:flex; gap:28px; align-items:center; padding:48px 0; border-bottom:1px solid var(--gray-200); }
.partner-avatar{ width:100px; height:100px; border-radius:50%; background:var(--gray-200); flex:none; }
.partner-stats{ display:flex; gap:36px; margin-top:14px; }
.partner-stats div{ font-size:0.8rem; color:var(--gray-500); }
.partner-stats b{ display:block; font-family:'Cormorant Garamond'; font-size:1.6rem; color:var(--gray-900); font-weight:400; }
.trust-chip{ display:inline-flex; align-items:center; gap:6px; background:var(--gray-900); color:#fff; padding:6px 14px; border-radius:var(--radius); font-size:0.74rem; letter-spacing:0.05em; margin-top:10px; }
.trust-chip::before{ content:"●"; color:var(--gold-light); font-size:0.6rem; }

footer.site{ background:var(--gray-900); color:var(--gray-400); padding:60px 0 30px; margin-top:80px; }
footer.site .cols{ display:grid; grid-template-columns:2fr 1fr 1fr 1fr; gap:40px; }
footer.site h5{ color:#fff; font-size:0.78rem; letter-spacing:0.1em; text-transform:uppercase; margin-bottom:16px; }
footer.site a{ display:block; font-size:0.85rem; padding:5px 0; color:var(--gray-400); }
footer.site a:hover{ color:#fff; }
footer.site .tagline{ font-family:'Cormorant Garamond'; font-style:italic; font-size:1.4rem; color:var(--gray-200); margin-top:14px; }
footer.site .bottom{ border-top:1px solid var(--gray-700); margin-top:44px; padding-top:22px; font-size:0.75rem; display:flex; justify-content:space-between; }

@media(max-width:900px){
  .grid{ grid-template-columns:1fr 1fr; }
  .detail-layout{ grid-template-columns:1fr; }
  .principle-strip{ flex-direction:column; }
  .principle-strip .p{ border-left:none; border-top:1px solid var(--gray-200); padding-top:20px; }
  .nav-links{ display:none; }
  .hero h1{ font-size:2.4rem; }
  footer.site .cols{ grid-template-columns:1fr 1fr; }

  .nav-row{ gap:10px; }
  .nav-actions{ gap:6px; }
  .nav-actions .btn-ghost{ display:none; }
  .nav-actions .btn-outline{ padding:9px 14px; font-size:0.72rem; }

  .cat-tabs{ flex-wrap:wrap; }
  .cat-tabs button{ flex:1 1 50%; }

  .search-fields{ flex-direction:column; align-items:stretch; padding:12px; }
  .search-fields .divider{ display:none; }
  .search-fields input, .search-fields select{ width:100%; padding:12px 10px; }
  .search-fields button.go{ width:100%; margin:6px 0 0; justify-content:center; }
  #view-search .search-fields{ flex-direction:row; flex-wrap:wrap; }
  #view-search .search-fields input{ flex:1 1 100%; }
  #view-search .search-fields select{ flex:1 1 auto; }
  #view-search .search-fields button.go{ width:auto; flex:1; margin:0; }
  #view-search .search-fields .btn-ghost{ flex:1; text-align:center; }
  .block-head{ flex-direction:column; align-items:flex-start; }
  .units-table-wrap{ overflow-x:auto; -webkit-overflow-scrolling:touch; }
  .units-table{ min-width:620px; }
  .partner-header{ flex-wrap:wrap; }
  .partner-stats{ flex-wrap:wrap; gap:20px; }
  .detail-actions-row{ flex-direction:column; }
  .detail-actions-row .btn{ width:100%; justify-content:center; }
}

/* ---- Phase 1 additions (i18n / product-layer, additive only) ---- */
.lang-switch{ display:flex; gap:2px; border:1px solid var(--gray-200); border-radius:var(--radius); overflow:hidden; margin-right:6px; }
.lang-switch button{ padding:8px 11px; font-size:0.72rem; letter-spacing:0.04em; color:var(--gray-500); transition:all .2s var(--ease); }
.lang-switch button.active{ background:var(--gray-900); color:#fff; }
.lang-switch button:hover:not(.active){ background:var(--gray-50); }
.rep-history{ font-size:0.82rem; color:var(--gray-700); margin:14px 0 20px; padding:12px 16px; background:var(--gray-50); border-radius:var(--radius); border:1px solid var(--gray-200); }
.direct-only-note{ font-size:0.82rem; color:var(--gray-500); background:var(--gray-50); border:1px solid var(--gray-200); border-radius:var(--radius); padding:14px; margin-top:14px; }
@media(max-width:900px){ .lang-switch{ margin-right:0; } }

/* ---------------- Legal content pages (Legal Guide, AL Manual) ---------------- */
.legal-page{ padding:56px 0 100px; }
.legal-header{ text-align:center; max-width:680px; margin:0 auto 56px; }
.legal-header .eyebrow{ font-size:0.72rem; letter-spacing:0.18em; text-transform:uppercase; color:var(--gold-dark); margin-bottom:14px; }
.legal-header h1{ font-size:2.6rem; margin-bottom:16px; }
.legal-header p{ color:var(--gray-500); line-height:1.6; }
.legal-header .disclaimer{ font-size:0.78rem; color:var(--gray-400); margin-top:18px; padding:12px 18px; background:var(--gray-50); border-radius:var(--radius); display:inline-block; }

.legal-layout{ display:grid; grid-template-columns:220px 1fr; gap:56px; max-width:1000px; margin:0 auto; align-items:start; }
.legal-toc{ position:sticky; top:24px; }
.legal-toc a{ display:block; padding:7px 0; font-size:0.82rem; color:var(--gray-500); border-left:2px solid transparent; padding-left:12px; }
.legal-toc a:hover, .legal-toc a.active{ color:var(--gray-900); border-left-color:var(--gold); }

.legal-section{ margin-bottom:56px; scroll-margin-top:24px; }
.legal-section .section-num{ font-size:0.72rem; letter-spacing:0.14em; text-transform:uppercase; color:var(--gold-dark); margin-bottom:8px; }
.legal-section h2{ font-size:1.7rem; margin-bottom:8px; }
.legal-section .section-intro{ color:var(--gray-500); margin-bottom:24px; line-height:1.6; }
.legal-card{ background:var(--gray-50); border:1px solid var(--gray-200); border-radius:var(--radius); padding:20px 22px; margin-bottom:14px; }
.legal-card h3{ font-size:1.05rem; font-family:'DM Sans'; font-weight:600; margin-bottom:8px; }
.legal-card p{ color:var(--gray-700); line-height:1.6; font-size:0.92rem; }
.legal-card p + p{ margin-top:8px; }
.legal-callout{ background:var(--gold-pale); border-left:3px solid var(--gold); border-radius:0 var(--radius) var(--radius) 0; padding:16px 20px; margin:20px 0; font-size:0.9rem; line-height:1.6; color:var(--gray-900); }
.legal-callout.link-out a{ color:var(--gold-dark); font-weight:600; text-decoration:underline; text-underline-offset:2px; }
.legal-table{ width:100%; border-collapse:collapse; font-size:0.88rem; margin:16px 0; }
.legal-table th, .legal-table td{ text-align:left; padding:9px 12px; border-bottom:1px solid var(--gray-200); }
.legal-table th{ font-weight:600; color:var(--gray-500); font-size:0.75rem; text-transform:uppercase; letter-spacing:0.04em; }
.legal-faq details{ border-bottom:1px solid var(--gray-200); padding:14px 0; }
.legal-faq summary{ cursor:pointer; font-weight:500; font-size:0.95rem; list-style:none; }
.legal-faq summary::-webkit-details-marker{ display:none; }
.legal-faq summary::before{ content:'+ '; color:var(--gold-dark); font-weight:700; }
.legal-faq details[open] summary::before{ content:'– '; }
.legal-faq p{ color:var(--gray-700); font-size:0.9rem; line-height:1.6; margin-top:10px; padding-left:16px; }
.legal-cta{ text-align:center; background:var(--gray-900); color:#fff; padding:48px 32px; border-radius:12px; margin-top:60px; }
.legal-cta h3{ font-size:1.5rem; margin-bottom:12px; color:#fff; }
.legal-cta p{ color:var(--gray-300); margin-bottom:22px; }
.legal-cta .btn{ display:inline-block; }
@media(max-width:820px){ .legal-layout{ grid-template-columns:1fr; } .legal-toc{ position:static; display:flex; flex-wrap:wrap; gap:4px 16px; margin-bottom:32px; } }


/* Z Find Rental Market — transaction intent is independent from subtype/channel */
.transaction-filter-block{
  display:flex;
  align-items:center;
  gap:16px;
  flex-wrap:wrap;
  margin-bottom:12px;
}

.transaction-tabs{
  display:inline-flex;
  align-items:center;
  padding:3px;
  border:1px solid var(--gray-200);
  border-radius:999px;
  background:var(--gray-50);
}

.transaction-tab{
  border:0;
  background:transparent;
  color:var(--gray-500);
  padding:8px 18px;
  border-radius:999px;
  font:inherit;
  font-size:0.82rem;
  font-weight:600;
  cursor:pointer;
  transition:all .18s ease;
}

.transaction-tab.active{
  background:var(--gray-900);
  color:#fff;
}

.rental-period-control{
  display:flex;
  align-items:center;
  gap:8px;
}

.rental-period-control label{
  font-size:0.76rem;
  color:var(--gray-500);
}

.rental-period-control select{
  border:1px solid var(--gray-200);
  border-radius:6px;
  padding:7px 9px;
  background:#fff;
  color:var(--gray-900);
  font:inherit;
  font-size:0.8rem;
}

@media (max-width:700px){
  .transaction-filter-block{
    align-items:stretch;
    gap:10px;
  }

  .transaction-tabs{
    width:100%;
  }

  .transaction-tab{
    flex:1;
  }

  .rental-period-control{
    width:100%;
    justify-content:space-between;
  }
}

/* ---------- responsive primary navigation ---------- */
.mobile-primary-nav{
  display:none;
  flex:none;
}

.menu-toggle{
  width:36px;
  height:36px;
  padding:0;
  flex:none;
  align-items:center;
  justify-content:center;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--white);
  color:var(--gray-900);
  font-size:1.15rem;
  line-height:1;
}

.menu-toggle:hover,
.menu-toggle:focus-visible{
  border-color:var(--gray-900);
}

.mobile-primary-menu[hidden]{
  display:none !important;
}

.mobile-primary-menu{
  position:fixed;
  z-index:80;
  top:84px;
  left:16px;
  right:16px;
  width:auto;
  max-height:calc(100vh - 100px);
  overflow-y:auto;
  padding:8px;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--white);
  box-shadow:var(--shadow-soft);
}

.mobile-primary-menu-item{
  display:block;
  width:100%;
  padding:13px 14px;
  border-radius:6px;
  text-align:left;
  color:var(--gray-700);
  font-size:0.9rem;
}

.mobile-primary-menu-item:hover,
.mobile-primary-menu-item:focus-visible,
.mobile-primary-menu-item.active{
  background:var(--gray-50);
  color:var(--gray-900);
}

@media(max-width:900px){
  .mobile-primary-nav{
    display:block;
  }

  .menu-toggle{
    display:inline-flex;
  }
}

@media(min-width:901px){
  .mobile-primary-nav{
    display:none !important;
  }
}

/* ---------- compact small-mobile header ---------- */
/*
  Keep every header capability available on narrow phones:
  primary menu, PT/EN/FR and Sign in.

  360px only needs reduced shell padding.
  <=340px additionally compacts the existing controls instead of
  hiding functionality.
*/
@media(max-width:380px){
  header.site .wrap{
    padding-left:16px;
    padding-right:16px;
  }
}

@media(max-width:340px){
  .nav-row{
    gap:8px;
  }

  .nav-actions{
    gap:4px;
  }

  .lang-switch button{
    padding:8px 5px;
    letter-spacing:0;
  }

  .nav-actions .btn-outline{
    padding-left:8px;
    padding-right:8px;
    letter-spacing:0.06em;
  }
}

/* ---------- DESIGN.1B international market-guide navigation ---------- */
.market-switch{
  display:flex;
  align-items:center;
  gap:8px;
  padding-right:14px;
  border-right:1px solid var(--gray-200);
}

.market-switch label{
  font-size:0.67rem;
  letter-spacing:0.12em;
  text-transform:uppercase;
  color:var(--gray-500);
  font-weight:500;
  white-space:nowrap;
}

.market-switch select{
  max-width:190px;
  padding:8px 26px 8px 9px;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--white);
  color:var(--gray-900);
  font:inherit;
  font-size:0.76rem;
}

.market-explorer{
  margin-top:28px;
  max-width:900px;
  min-height:76px;
  padding:16px 18px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:24px;
  border:1px solid var(--gray-200);
  background:rgba(255,255,255,0.82);
  box-shadow:var(--shadow-soft);
}

.market-explorer .eyebrow{
  margin-bottom:5px;
}

.market-explorer-copy p{
  color:var(--gray-500);
  font-size:0.82rem;
  line-height:1.45;
  max-width:520px;
}

.market-explorer select{
  width:270px;
  flex:none;
  padding:11px 34px 11px 12px;
  border:1px solid var(--gray-300);
  border-radius:var(--radius);
  background:var(--white);
  color:var(--gray-900);
  font:inherit;
  font-size:0.84rem;
}

.market-explorer + .searchbar{
  margin-top:18px;
}

.lang-menu{
  position:relative;
  flex:none;
}

.lang-menu-summary{
  min-width:58px;
  height:38px;
  padding:0 9px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:6px;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--white);
  color:var(--gray-900);
  font-size:0.75rem;
  font-weight:500;
  letter-spacing:0.05em;
  cursor:pointer;
  list-style:none;
}

.lang-menu-summary::-webkit-details-marker{
  display:none;
}

.lang-menu[open] .lang-menu-summary,
.lang-menu-summary:hover,
.lang-menu-summary:focus-visible{
  border-color:var(--gray-900);
}

.lang-menu-panel{
  position:absolute;
  z-index:90;
  top:calc(100% + 10px);
  right:0;
  width:210px;
  padding:7px;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--white);
  box-shadow:var(--shadow-soft);
}

.lang-menu-panel button{
  width:100%;
  min-height:38px;
  padding:8px 10px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  border-radius:6px;
  color:var(--gray-700);
  text-align:left;
  font-size:0.84rem;
}

.lang-menu-panel button:hover:not(:disabled),
.lang-menu-panel button:focus-visible:not(:disabled),
.lang-menu-panel button.active{
  background:var(--gray-50);
  color:var(--gray-900);
}

.lang-menu-panel button:disabled{
  cursor:not-allowed;
  opacity:0.52;
}

.lang-menu-panel small{
  color:var(--gray-400);
  font-size:0.67rem;
  letter-spacing:0.05em;
  white-space:nowrap;
}

@media(max-width:1180px){
  .market-switch label{
    display:none;
  }

  .market-switch select{
    max-width:155px;
  }
}

@media(max-width:900px){
  .nav-actions .market-switch{
    display:none;
  }
}

@media(max-width:700px){
  .hero{
    padding-top:56px;
  }

  .hero h1{
    font-size:2.75rem;
  }

  .market-explorer{
    align-items:stretch;
    flex-direction:column;
    gap:12px;
  }

  .market-explorer select{
    width:100%;
  }
}

/* ---------- DESIGN.1C-R1 rhythm ---------- */
.sr-only{
  position:absolute !important;
  width:1px !important;
  height:1px !important;
  padding:0 !important;
  margin:-1px !important;
  overflow:hidden !important;
  clip:rect(0,0,0,0) !important;
  white-space:nowrap !important;
  border:0 !important;
}

.cat-tabs.design-balanced-categories{
  display:grid;
  grid-template-columns:repeat(4,minmax(0,1fr));
}

.cat-tabs.design-balanced-categories .cat-tab{
  min-width:0;
  padding-left:16px;
  padding-right:16px;
  white-space:normal;
  line-height:1.3;
}

.search-fields.search-fields-balanced{
  display:grid;
  grid-template-columns:
    auto 1px
    minmax(220px,2.2fr) 1px
    minmax(130px,1fr) 1px
    minmax(140px,1fr)
    auto;
  align-items:center;
  min-height:72px;
  padding:12px;
}

.search-fields-balanced .transaction-filter-inline{
  display:flex;
  align-items:center;
  gap:8px;
  padding:0 14px 0 0;
  border:0;
}

.search-fields-balanced .transaction-tabs{
  flex:none;
  margin:0;
}

.search-fields-balanced .rental-period-control{
  margin:0;
}

.search-fields-balanced .rental-period-control select{
  min-width:116px;
  height:38px;
  padding:0 28px 0 10px;
}

.search-fields-balanced .divider{
  height:30px;
  align-self:center;
}

.search-fields-balanced > input,
.search-fields-balanced > select{
  width:100%;
  min-width:0;
}

.search-fields-balanced .go{
  min-height:44px;
  margin-left:10px;
  padding-left:28px;
  padding-right:28px;
}

.principle-strip.design-balanced-principles{
  display:grid;
  grid-template-columns:repeat(3,minmax(0,1fr));
  margin-top:56px;
  align-items:stretch;
}

.principle-strip.design-balanced-principles .p{
  padding:28px 28px 30px;
  display:flex;
  flex-direction:column;
  align-items:flex-start;
}

.principle-strip.design-balanced-principles .p .num{
  display:block;
  margin-bottom:10px;
}

.principle-strip.design-balanced-principles .p h4{
  min-height:64px;
  margin:0 0 10px;
  display:flex;
  align-items:flex-start;
}

.principle-strip.design-balanced-principles .p p{
  margin:0;
  max-width:340px;
  line-height:1.55;
}

#home-status.design-home-status{
  margin-top:56px !important;
  margin-bottom:72px !important;
  padding-top:54px !important;
  padding-bottom:54px !important;
}

#home-status.design-home-status #home-status-body{
  max-width:680px;
  margin-left:auto;
  margin-right:auto;
  line-height:1.55;
}

@media(max-width:900px){
  .cat-tabs.design-balanced-categories{
    grid-template-columns:repeat(2,minmax(0,1fr));
  }

  .search-fields.search-fields-balanced{
    display:flex;
    flex-wrap:wrap;
    gap:10px;
    padding:14px;
  }

  .search-fields-balanced .transaction-filter-inline{
    width:100%;
    justify-content:space-between;
    padding:0 0 10px;
  }

  .search-fields-balanced .transaction-divider,
  .search-fields-balanced .divider{
    display:none;
  }

  .search-fields-balanced > input{
    flex:1 1 100%;
  }

  .search-fields-balanced > select{
    flex:1 1 180px;
  }

  .search-fields-balanced .go{
    flex:1 1 160px;
    margin-left:0;
  }

  .principle-strip.design-balanced-principles{
    grid-template-columns:1fr;
  }

  .principle-strip.design-balanced-principles .p h4{
    min-height:0;
  }
}

@media(max-width:560px){
  .cat-tabs.design-balanced-categories{
    grid-template-columns:1fr;
  }

  .search-fields-balanced .transaction-filter-inline{
    align-items:stretch;
    flex-direction:column;
  }

  .search-fields-balanced .transaction-tabs,
  .search-fields-balanced .rental-period-control,
  .search-fields-balanced .rental-period-control select{
    width:100%;
  }

  .principle-strip.design-balanced-principles{
    margin-top:42px;
  }

  #home-status.design-home-status{
    margin-top:42px !important;
    margin-bottom:56px !important;
  }
}

/* ---------- DESIGN.1D-R1 approved Atlantic hero ---------- */
header.site .wrap.nav-row{
  max-width:1440px;
  padding-left:40px;
  padding-right:40px;
  gap:28px;
}

header.site .nav-links{
  gap:24px;
}

.hero > .wrap{
  position:relative;
  isolation:isolate;
}

.hero > .wrap > :not(.hero-atlantic-visual){
  position:relative;
  z-index:2;
}

.hero-atlantic-visual{
  position:absolute;
  z-index:0;
  top:-70px;
  right:-92px;
  width:min(64vw,900px);
  height:690px;
  overflow:hidden;
  pointer-events:none;
  opacity:.88;
  -webkit-mask-image:linear-gradient(
    to right,
    transparent 0%,
    rgba(0,0,0,.18) 10%,
    rgba(0,0,0,.72) 24%,
    #000 38%,
    #000 100%
  );
  mask-image:linear-gradient(
    to right,
    transparent 0%,
    rgba(0,0,0,.18) 10%,
    rgba(0,0,0,.72) 24%,
    #000 38%,
    #000 100%
  );
}

.hero-atlantic-visual::after{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(
    to bottom,
    transparent 0%,
    transparent 76%,
    var(--gray-50) 100%
  );
}

.hero-atlantic-visual img{
  width:100%;
  height:100%;
  object-fit:cover;
  object-position:50% 48%;
  filter:saturate(.88) contrast(.98);
}

.hero h1{
  max-width:690px;
}

.hero p.lead{
  max-width:590px;
}

@media(max-width:1280px){
  header.site .wrap.nav-row{
    padding-left:24px;
    padding-right:24px;
    gap:20px;
  }

  header.site .nav-links{
    gap:18px;
  }

  header.site .nav-actions{
    gap:10px;
  }

  header.site .nav-actions .btn{
    padding-left:18px;
    padding-right:18px;
  }

  .hero-atlantic-visual{
    right:-40px;
    width:min(60vw,760px);
    opacity:.74;
  }
}

@media(max-width:980px){
  .hero-atlantic-visual{
    right:-120px;
    width:70vw;
    opacity:.34;
  }
}

@media(max-width:760px){
  header.site .wrap.nav-row{
    padding-left:20px;
    padding-right:20px;
  }

  .hero-atlantic-visual{
    display:none;
  }

  .hero h1,
  .hero p.lead{
    max-width:none;
  }
}


/* ---------- COUNTRY MARKET A1 organization + SEO foundation ---------- */
.market-foundation-page{
  min-height:calc(100vh - 72px);
  background:#fff;
}
.market-foundation-hero{
  position:relative;
  overflow:hidden;
  border-bottom:1px solid var(--gray-100);
  background:
    radial-gradient(circle at 82% 28%, rgba(188,151,83,.08), transparent 30%),
    linear-gradient(180deg, #fff 0%, #fcfaf6 100%);
}
.market-foundation-hero-grid{
  min-height:430px;
  display:grid;
  grid-template-columns:minmax(0,1.05fr) minmax(320px,.95fr);
  align-items:center;
  gap:56px;
  padding-top:56px;
  padding-bottom:56px;
}
.market-foundation-copy{
  position:relative;
  z-index:2;
  max-width:760px;
}
.market-foundation-copy h1{
  margin:10px 0 18px;
  font-family:var(--serif);
  font-size:clamp(2.4rem,4.2vw,4.4rem);
  line-height:.98;
  letter-spacing:-.035em;
}
.market-back-home{
  margin-bottom:28px;
  padding-left:0;
}
.market-foundation-actions{
  display:flex;
  flex-wrap:wrap;
  gap:10px;
  margin-top:24px;
}
.market-map-slot{
  min-height:320px;
  width:100%;
  border-radius:28px;
  opacity:.72;
  background:
    radial-gradient(circle at 48% 48%, rgba(188,151,83,.11), transparent 34%),
    linear-gradient(135deg, rgba(246,241,230,.72), rgba(255,255,255,.34));
}
.market-foundation-section{
  padding-top:56px;
  padding-bottom:56px;
  border-bottom:1px solid var(--gray-100);
}
.market-foundation-section .block-head{
  margin-bottom:0;
}
.market-foundation-section .block-head p{
  margin-top:8px;
  max-width:760px;
  color:var(--gray-500);
  line-height:1.65;
}
#market-featured-root,
#market-search-root{
  min-height:1px;
}
@media(max-width:900px){
  .market-foundation-hero-grid{
    grid-template-columns:1fr;
    min-height:auto;
    gap:18px;
    padding-top:34px;
    padding-bottom:38px;
  }
  .market-map-slot{
    min-height:180px;
  }
}
@media(max-width:760px){
  .market-map-slot{
    display:none;
  }
  .market-foundation-section{
    padding-top:40px;
    padding-bottom:40px;
  }
}

/* ---------- COUNTRY MARKET A2 exact map visual ---------- */
.market-map-slot{
  position:relative;
  display:flex;
  align-items:center;
  justify-content:center;
  overflow:hidden;
  isolation:isolate;
  background:
    radial-gradient(circle at 50% 48%, rgba(197,166,111,.13), transparent 46%),
    linear-gradient(135deg, rgba(250,246,237,.64), rgba(255,255,255,.18));
}
.market-map-slot::after{
  content:"";
  position:absolute;
  inset:0;
  pointer-events:none;
  background:linear-gradient(90deg, rgba(255,255,255,.84) 0%, transparent 18%, transparent 82%, rgba(255,255,255,.70) 100%);
  z-index:2;
}
.market-map-visual{
  position:relative;
  z-index:1;
  width:100%;
  height:100%;
  max-width:760px;
  max-height:430px;
  object-fit:contain;
  opacity:.90;
  filter:drop-shadow(0 20px 34px rgba(128,98,47,.13));
  -webkit-mask-image:linear-gradient(to right, transparent 0%, #000 13%, #000 88%, transparent 100%);
  mask-image:linear-gradient(to right, transparent 0%, #000 13%, #000 88%, transparent 100%);
}
@media(max-width:900px){
  .market-map-visual{
    max-height:260px;
  }
}

/* ---------- COUNTRY MARKET A3 six weekly featured slots ---------- */
#market-featured-root{
  display:grid;
  grid-template-columns:repeat(3,minmax(0,1fr));
  gap:18px;
  margin-top:26px;
}
.market-featured-slot{
  position:relative;
  min-width:0;
  min-height:270px;
  border-radius:18px;
}
.market-featured-card{
  border:1px solid rgba(180,148,91,.34);
  background:
    linear-gradient(180deg,rgba(252,248,240,.82),rgba(255,255,255,.98));
  box-shadow:0 18px 44px rgba(90,68,35,.07);
}
.market-featured-card .card{
  height:100%;
  margin:0;
  border:0;
  border-radius:18px;
  box-shadow:none;
  background:transparent;
}
.market-featured-card .thumb .badge{
  display:none;
}
.market-featured-label{
  position:absolute;
  z-index:4;
  top:14px;
  left:14px;
  display:inline-flex;
  align-items:center;
  min-height:26px;
  padding:5px 10px;
  border:1px solid rgba(171,136,75,.34);
  border-radius:999px;
  background:rgba(255,252,245,.94);
  color:#8d6a34;
  font-size:11px;
  font-weight:700;
  letter-spacing:.07em;
  text-transform:uppercase;
  box-shadow:0 8px 20px rgba(108,78,30,.08);
}
.market-featured-empty{
  display:flex;
  align-items:flex-end;
  gap:16px;
  padding:22px;
  border:1px dashed rgba(171,136,75,.34);
  background:
    radial-gradient(circle at 82% 18%,rgba(197,166,111,.10),transparent 35%),
    linear-gradient(145deg,#fffdfa,#faf6ee);
  color:var(--gray-500);
}
.market-featured-empty strong{
  display:block;
  margin-bottom:7px;
  color:var(--gray-700);
  font-family:'Cormorant Garamond',serif;
  font-size:20px;
  font-weight:500;
}
.market-featured-empty p{
  margin:0;
  line-height:1.55;
}
.market-featured-slot-number{
  flex:0 0 auto;
  color:rgba(151,113,52,.60);
  font-family:'Cormorant Garamond',serif;
  font-size:30px;
  line-height:1;
}
.market-featured-loading{
  min-height:210px;
  opacity:.72;
}
.market-featured-error{
  border-style:solid;
}
@media(max-width:980px){
  #market-featured-root{
    grid-template-columns:repeat(2,minmax(0,1fr));
  }
}
@media(max-width:640px){
  #market-featured-root{
    grid-template-columns:1fr;
  }
  .market-featured-slot{
    min-height:230px;
  }
}

/* ---------- COUNTRY MARKET A4 sovereign scoped search ---------- */
.market-scoped-search{
  padding:18px;
  border:1px solid rgba(180,148,91,.28);
  border-radius:18px;
  background:linear-gradient(
    145deg,
    rgba(255,253,249,.98),
    rgba(249,245,236,.90)
  );
  box-shadow:0 16px 36px rgba(90,68,35,.05);
}
.market-search-fields{
  display:grid;
  grid-template-columns:
    minmax(130px,.7fr)
    minmax(220px,1.6fr)
    minmax(190px,1fr)
    auto;
  gap:10px;
  align-items:center;
}
.market-search-fields input,
.market-search-fields select{
  width:100%;
  min-height:46px;
  border:1px solid var(--gray-200);
  border-radius:10px;
  background:#fff;
  padding:0 12px;
  color:var(--gray-800);
  font:inherit;
}
.market-search-fields .btn{
  min-height:46px;
  justify-content:center;
  white-space:nowrap;
}
.market-search-pending{
  padding:24px;
  border:1px dashed rgba(171,136,75,.40);
  border-radius:16px;
  background:rgba(252,248,240,.72);
}
.market-search-pending strong{
  display:block;
  margin-bottom:8px;
  font-family:'Cormorant Garamond',serif;
  font-size:22px;
  font-weight:500;
  color:var(--gray-800);
}
.market-search-pending p{
  margin:0;
  max-width:760px;
  color:var(--gray-500);
  line-height:1.6;
}
@media(max-width:900px){
  .market-search-fields{
    grid-template-columns:1fr 1fr;
  }
}
@media(max-width:620px){
  .market-search-fields{
    grid-template-columns:1fr;
  }
}

/* ============================================================
   PHASE B1 — SEARCH RESULTS LAYOUT FOUNDATION
   Structural shell only. The right rail is intentionally reserved
   and carries no paid/commercial content until the dedicated B3/B4
   commercial-placement phase.
   ============================================================ */
#view-search .search-results-layout{
  display:grid;
  grid-template-columns:minmax(0,2fr) minmax(280px,1fr);
  gap:32px;
  align-items:start;
}

#view-search .search-results-main{
  min-width:0;
}

#view-search #search-grid{
  grid-template-columns:1fr;
}

#view-search .search-results-aside{
  min-width:0;
  min-height:1px;
}

@media(max-width:900px){
  #view-search .search-results-layout{
    grid-template-columns:1fr;
    gap:22px;
  }
}

/* ============================================================
   PHASE B2 — HORIZONTAL ORGANIC SEARCH RESULT ROWS
   ============================================================ */
#view-search .search-result-row.card{
  display:grid;
  grid-template-columns:minmax(190px,32%) minmax(0,1fr);
  min-height:190px;
  height:auto;
  margin:0;
  overflow:hidden;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:#fff;
  box-shadow:none;
  transform:none;
}

#view-search .search-result-row.card:hover{
  transform:none;
}

#view-search .search-result-thumb{
  position:relative;
  min-height:190px;
  overflow:hidden;
  background:linear-gradient(
    135deg,
    var(--gray-200),
    var(--gray-100)
  );
}

#view-search .search-result-thumb img{
  display:block;
  width:100%;
  height:100%;
  min-height:190px;
  object-fit:cover;
}

#view-search .search-result-thumb .badge{
  position:absolute;
  top:14px;
  left:14px;
  z-index:1;
}

#view-search .search-result-body{
  min-width:0;
  padding:24px 26px;
  display:flex;
  flex-direction:column;
  justify-content:center;
}

#view-search .search-result-body .price{
  font-family:'Cormorant Garamond';
  font-size:1.55rem;
}

#view-search .search-result-title{
  margin-top:8px;
  font-size:1rem;
  line-height:1.35;
  color:var(--gray-900);
}

#view-search .search-result-body .loc{
  margin-top:5px;
  font-size:.82rem;
  color:var(--gray-500);
}

#view-search .search-result-body .meta{
  display:flex;
  flex-wrap:wrap;
  gap:14px;
  margin-top:16px;
  padding-top:13px;
  border-top:1px solid var(--gray-100);
  font-size:.78rem;
  color:var(--gray-500);
}

#view-search .search-result-body .facts-count{
  margin-top:11px;
  font-size:.72rem;
  color:var(--gold-dark);
  letter-spacing:.04em;
}

@media(max-width:700px){
  #view-search .search-result-row.card{
    grid-template-columns:1fr;
    min-height:0;
  }

  #view-search .search-result-thumb,
  #view-search .search-result-thumb img{
    min-height:190px;
    height:190px;
  }

  #view-search .search-result-body{
    padding:20px;
  }
}

/* ============================================================
   PHASE B3/B4 — SEARCH FEATURED RAIL
   ============================================================ */
#view-search .search-results-aside[aria-hidden="true"]{
  display:none;
}

#view-search .search-results-aside[aria-hidden="false"]{
  display:block;
}

#view-search .search-featured-rail{
  position:sticky;
  top:24px;
}

#view-search .search-featured-rail-head{
  margin-bottom:14px;
  padding-bottom:10px;
  border-bottom:1px solid var(--gray-200);
}

#view-search .search-featured-slots{
  display:grid;
  grid-template-columns:1fr;
  gap:18px;
}

#view-search .search-featured-slot{
  min-width:0;
}

#view-search .search-featured-label{
  display:block;
  margin-bottom:8px;
  font-size:.7rem;
  font-weight:700;
  letter-spacing:.12em;
  text-transform:uppercase;
  color:var(--gold-dark);
}

#view-search .search-featured-card .card{
  width:100%;
  margin:0;
}

#view-search .search-featured-card .card .thumb{
  height:150px;
}

#view-search .search-featured-empty{
  min-height:120px;
  padding:18px;
  display:flex;
  gap:14px;
  align-items:flex-start;
  border:1px solid var(--gray-200);
  border-radius:var(--radius);
  background:var(--gray-50);
}

#view-search .search-featured-slot-number{
  flex:0 0 auto;
  font-family:'Cormorant Garamond';
  font-size:1.15rem;
  color:var(--gold-dark);
}

#view-search .search-featured-empty strong{
  display:block;
  font-size:.86rem;
  color:var(--gray-800);
}

#view-search .search-featured-empty p{
  margin-top:6px;
  font-size:.76rem;
  line-height:1.5;
  color:var(--gray-500);
}

@media(max-width:900px){
  #view-search .search-featured-rail{
    position:static;
  }

  #view-search .search-featured-slots{
    grid-template-columns:repeat(3,minmax(0,1fr));
  }
}

@media(max-width:700px){
  #view-search .search-featured-slots{
    grid-template-columns:1fr;
  }
}

/* PHASE B5 — Search organic pagination */
#view-search .search-results-pagination{
  display:flex;
  align-items:center;
  justify-content:center;
  gap:14px;
  margin-top:24px;
  padding-top:20px;
  border-top:1px solid var(--gray-200);
}

#view-search .search-pagination-button{
  min-height:40px;
  padding:9px 16px;
  border:1px solid var(--gray-300);
  border-radius:999px;
  background:white;
  color:var(--gray-800);
  font:inherit;
  font-size:0.84rem;
  cursor:pointer;
}

#view-search .search-pagination-button:hover:not(:disabled){
  border-color:var(--gold);
  color:var(--gold-dark);
}

#view-search .search-pagination-button:disabled{
  opacity:.42;
  cursor:default;
}

#view-search .search-pagination-label{
  min-width:110px;
  text-align:center;
  color:var(--gray-600);
  font-size:0.84rem;
}

@media(max-width:700px){
  #view-search .search-results-pagination{
    gap:9px;
  }

  #view-search .search-pagination-button{
    padding:8px 11px;
  }

  #view-search .search-pagination-label{
    min-width:0;
  }
}
