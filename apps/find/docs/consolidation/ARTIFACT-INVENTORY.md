# Artifact Inventory — Sprint A Consolidation

Status: Sprint A locally complete — GitHub activation pending.

## apps/zfind-web/src/ — 9 source artifacts (corrected from 8)

| # | File | SHA-256 (short) | Role |
|---|---|---|---|
| 1 | `head_top.txt` | — | DOCTYPE, `<head>` opener, favicon, font links |
| 2 | `css_block.txt` | — | Full stylesheet |
| 3 | `body.html` | `e797fcca...` | HTML body, contains 3× `__PATH_D__` placeholder |
| 4 | **`path_data.txt`** | `d7f40ac6...` | **9th artifact, added in Sprint A** — traced logo SVG path (1657 bytes). Previously a hidden, undeclared build input; now explicit. Byte-identical to the value used in every prior approved build. |
| 5 | `geography.js` | `508f6c47...` | Geography bounded context (read-only) |
| 6 | `db.js` | — | Registry/Marketplace/Trust/Data/Intelligence fixture |
| 7 | `i18n.js` | — | PT/EN/FR interface strings |
| 8 | `viewmodels.js` | — | View-model/selector layer |
| 9 | `app.js` | — | Router, render, event wiring |

## packages/geography/

- `geography.js` — byte-identical duplicate of `apps/zfind-web/src/geography.js` (SHA-256 `508f6c47...`, verified). Duplication is intentional and temporary for Sprint A; deduplication is Sprint B scope, not done here.

## packages/import-engine/ (v2, canonical)

9 files copied verbatim from the last approved delivery, checksums verified against source: `import-engine-v2.js`, `canonical-store-v2.js`, `canonical-store-seed.js`, `batch-lifecycle.js`, `review-workflow.js`, `matching-policy.js`, `geography-port.js`, `persistence.js`, plus `adapters/{pt,fr}-adapter.js` and `fixtures/{pt,fr}-fixture.js`.

## archive/import-engine-v1/

Full v1 proof-of-concept, preserved intact (`import-engine.js`, `canonical-store.js`, `test.js`, adapters, fixtures). See `ARCHIVED.md` in that directory.

## tests/

- `tests/integration/import-engine/test-v2.js` — require() paths updated to point to `packages/import-engine/` (path-only change, zero logic touched). 55/55 passing.
- `tests/browser/zfind-web/browser_test.js` — file-path reference updated to point to `apps/zfind-web/dist/z-find-prototype.html`. 15/15 scenarios passing.
- `archive/import-engine-v1/test.js` — unchanged, 42/42 passing.

## Build artifact

- `apps/zfind-web/dist/z-find-prototype.html` — generated by `apps/zfind-web/scripts/build.js`. SHA-256 `adfb78be...`, byte-for-byte identical to the previously approved standalone prototype.

## Discarded (not migrated, not archived)

- 9 `z-operating-system-*.zip` historical delivery zips — content already committed to the real `z-operating-system` GitHub repository; git history is the correct place for that record, not a second copy here.
