import {
  discoverOfficialAttachments,
  downloadOfficialDocument,
} from "../../documents";

import type {
  DownloadedOfficialDocument,
} from "../../documents";

export async function discoverBmwOfficialDocument(
  articleUrl: string,
): Promise<DownloadedOfficialDocument | null> {
  void articleUrl;
  void discoverOfficialAttachments;
  void downloadOfficialDocument;

  return null;
}