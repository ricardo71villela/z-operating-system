{
  "name": "z-studio-commercial-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Z Studio — privileged commercial verification runtime",
  "type": "module",
  "scripts": {
    "check": "node --check lib/config.js && node --check lib/store-products.js && node --check lib/apple-signed-data.js && node --check lib/apple-server-api.js && node --check lib/apple-notifications.js && node --check lib/commercial-event-adapter.js && node --check lib/commercial-writer-client.js && node --check lib/apple-device-reconcile-http.js && node --check api/apple/reconcile.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@apple/app-store-server-library": "3.1.0"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
