{
  "name": "zos-platform",
  "version": "0.1.0",
  "private": true,
  "description": "ZOS Platform monorepo — Z Mobility, Z Jobs, Z Find and shared platform capabilities.",
  "workspaces": [
    "apps/jobs/apps/*",
    "apps/jobs/packages/*",
    "apps/jobs/tools",
    "domains/*",
    "packages/*",
    "tooling/*",
    "apps/find",
    "apps/mobility",
    "apps/mobility/packages/*"
  ],
  "scripts": {
    "jobs:typecheck": "npm run typecheck --workspace=@zjobs/domain && npm run typecheck --workspace=@zjobs/api",
    "jobs:test": "npm run test --workspace=@zjobs/domain",
    "check": "npm run jobs:typecheck",
    "test": "npm run jobs:test",
    "build": "echo ZOS-build-pending",
    "find:check": "npm run check --workspace=z-find-platform",
    "find:test": "npm run test --workspace=z-find-platform",
    "find:build": "npm run build:zfind --workspace=z-find-platform && npm run build:admin --workspace=z-find-platform && npm run build:partner --workspace=z-find-platform",
    "mobility:typecheck": "npm run typecheck --workspace=z-mobility-next",
    "mobility:test": "npm run test:automotive --workspace=z-mobility-next",
    "mobility:build": "npm run build --workspace=z-mobility-next"
  }
}
