create schema if not exists zos;

comment on schema zos is 'Shared ZOS authorities and cross-vertical capabilities.';

create schema if not exists platform_internal;

comment on schema platform_internal is 'Internal integration and operational infrastructure for ZOS.';
